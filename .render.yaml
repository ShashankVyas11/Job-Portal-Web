services:
  - type: web
    name: job-portal
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn run:app
    env: python
    pythonVersion: 3.11
    envVars:
      - key: SECRET_KEY
        value: your-secret-value
    autoDeploy: true
