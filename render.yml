services:
  - type: web
    name: job-portal
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn run:app
    envVars:
      - key: SECRET_KEY
        value: ${SECRET_KEY} 